<nav class="user-nav">
    {% if app.user %}

        <span>Hello {{ app.user.pseudo }}</span>
        <a href="{{ path('app_logout') }}" title="Logout">Logout</a>

    {% else %}

        <a href="{{ path('app_register') }}" title="Create account">Register</a>
        <a href="{{ path('app_login') }}" title="Login">Login</a>

    {% endif %}
</nav>
</div>
<nav class="header-nav">

    <a href="{{ path('main_home') }}" title="Go back to homepage">Home</a>
    <a href="{{ path('wish_list') }}" title="Go to the wish list">All wishes</a>
    {% if app.user %}
        <a href="{{ path('wish_create') }}" title="Make a wish">Add yours!</a>
    {% endif %}
    <a href="{{ path('main_about_us') }}" title="Learn more about us">About us</a>
</nav>

